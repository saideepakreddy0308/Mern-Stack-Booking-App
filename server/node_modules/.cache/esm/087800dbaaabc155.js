let expressJwt,Hotel;_aaa‍.x([["requireSignin",()=>requireSignin],["hotelOwner",()=>hotelOwner]]);_aaa‍.w("express-jwt",[["default",["expressJwt"],function(v){expressJwt=v}]]);_aaa‍.w("../models/hotel",[["default",["Hotel"],function(v){Hotel=v}]]);


// req.user
       const requireSignin = expressJwt({
  // secret, expiryDate
  secret: process.env.JWT_SECRET,
  algorithms: ["HS256"],
});

       const hotelOwner = async (req, res, next) => {
  let hotel = await Hotel.findById(req.params.hotelId).exec();
  let owner = hotel.postedBy._id.toString() === req.user._id.toString();
  if (!owner) {
    return res.status(403).send("Unauthorized");
  }
  next();
};
